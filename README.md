# hobbit
> Habits tracker to prepare my nodejs skills before i started nestjs

## Purpose 
Learn how to search for information about a new technology without knowledge (I mean TS+NodeJS). 

## Base commands 

Стартанул проект: 
```
npm init -y // создает package.json (yes all)

npm i -D typescript ts-node @types/node // добавляем зависимости в режиме "онли фор дев" + тайпскрипт компилятор тс ту джиэс + тс-нод позволяет запускать тс напрямую + типы для ноды поскольку тс не знает что такое os fs и тд 

npx tsc --init // создает файл конфигурации для тс 
```